import React from 'react';

export default function CardMealsDetails() {
  return (
    <div>
      <img data-testid="recipe-photo" alt="a" />
      <title data-testid="recipe-title">a</title>
      <span data-testid="recipe-category">a</span>
      {/* <span data-testid={ `${index}-ingredient-name-and-measure` }>a</span> */}
      <span data-testid="instructions">a</span>
      <span data-testid="video">a</span>
    </div>
  );
}
